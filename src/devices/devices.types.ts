export type DeviceDto = {
	ip: string;
	title: string;
	lastActiveDate: string;
	deviceId: string;
};

export type DeviceType = {
	ip: string;
	title: string;
	lastActiveDate: number;
	deviceId: string;
};

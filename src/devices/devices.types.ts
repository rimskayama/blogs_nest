export type DeviceViewDto = {
	ip: string;
	title: string;
	lastActiveDate: string;
	deviceId: string;
};

export type DeviceDto = {
	ip: string;
	title: string;
	lastActiveDate: number;
	deviceId: string;
};
